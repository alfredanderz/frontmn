version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd FRONTEND
        - npm install
    build:
      commands:
        - cd FRONTEND
        - npm run build
  artifacts:
    baseDirectory: FRONTEND/dist
    files:
      - "**/*"
  cache:
    paths:
      - FRONTEND/node_modules/**/*
